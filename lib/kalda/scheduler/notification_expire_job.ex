defmodule Kalda.Scheduler.NotificationExpireJob do
  # def run do
  # get_users()
  # |> Enum.each(fn user ->
  #   Kalda.Forums.get_notifications(user, limit: 50)
  # checks if sent and 2 weeks old
  # mark as expired
  # end)
  # end
end
