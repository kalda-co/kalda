<%# edit %>
<h1>Edit Post</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_post_path(@conn, :update, @post)) %>

<span><%= link "Back", to: Routes.admin_post_path(@conn, :index) %></span>
